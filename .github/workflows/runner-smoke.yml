name: Runner Smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: [self-hosted, kamiyo-ci]
    steps:
      - name: Print diagnostics
        run: |
          echo "runner smoke test"
          uname -a
          date -u +"%Y-%m-%dT%H:%M:%SZ"
