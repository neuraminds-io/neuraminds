# Base Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../deployment.yaml
  - ../secrets.yaml

commonLabels:
  app.kubernetes.io/name: neuraminds
  app.kubernetes.io/component: api
  app.kubernetes.io/managed-by: kustomize
