# Neuraminds Secrets Configuration
#
# In production, use a secrets manager like:
# - AWS Secrets Manager with External Secrets Operator
# - HashiCorp Vault
# - Sealed Secrets
#
# This file is a template - DO NOT commit actual secrets

---
# External Secrets Operator configuration (recommended)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: neuraminds-secrets
  namespace: neuraminds
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secrets-manager
  target:
    name: neuraminds-secrets
    creationPolicy: Owner
  data:
    - secretKey: database-url
      remoteRef:
        key: neuraminds/production
        property: database_url
    - secretKey: redis-url
      remoteRef:
        key: neuraminds/production
        property: redis_url
    - secretKey: jwt-secret
      remoteRef:
        key: neuraminds/production
        property: jwt_secret

---
# ConfigMap for non-sensitive configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: neuraminds-config
  namespace: neuraminds
data:
  base-rpc-url: "https://mainnet.base.org"
  base-chain-id: "8453"

---
# Service Account
apiVersion: v1
kind: ServiceAccount
metadata:
  name: neuraminds-api
  namespace: neuraminds
  annotations:
    # For AWS IAM roles for service accounts
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/neuraminds-api

---
# Example Secret (for local development only - use External Secrets in production)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: neuraminds-secrets
#   namespace: neuraminds
# type: Opaque
# data:
#   database-url: <base64-encoded>
#   redis-url: <base64-encoded>
#   jwt-secret: <base64-encoded>
#   keeper-keypair: <base64-encoded>
